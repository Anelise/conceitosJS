/**
Quando vocês quer percorrer os atributos de um objeto você pode usar o 'for in'
**/

let umobj = {nome:'Maria',idade:'20',sexo:'feminio'}
for( chave in umobj){
	console.log(chave, umobj[chave]);
}
